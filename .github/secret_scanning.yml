# GitHub secret scanning — push protection guidance for maintainers.
# Enable in repo settings: Settings → Code security → Secret scanning / Push protection.
#
# This file documents expected configuration; GitHub manages scanning server-side.

version: 1
push_protection: recommended
secret_scanning: enabled
